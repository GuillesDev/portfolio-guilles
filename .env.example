# NVIDIA Build / NIM
# Keep the real value only in .env locally and in Vercel Environment Variables.
NVIDIA_API_KEY=nvapi-your-key-here

# Optional: override the hosted model without changing the endpoint.
NVIDIA_MODEL=microsoft/phi-4-mini-instruct
